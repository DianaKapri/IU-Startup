# ШколаПлан — автоматизированное составление школьного расписания

## Описание продукта

«ШколаПлан» — инструмент для автоматизированного составления школьного расписания с поддержкой индивидуальных учебных планов (ИУП) и деления на группы.

## Гипотеза

Внедрение интеллектуального модуля для работы с ИУП и делением на группы сократит время составления расписания с 14 дней до 1–2 дней и позволит найти реальных покупателей.

## Целевая аудитория

Завучи частных и государственных школ в возрасте 27–60 лет.

## Проблема клиента

Текущие процессы и ограничения:
* ручное распределение учеников по индивидуальным траекториям;
* существующие решения не справляются с пересечениями групп и ИУП;
* на составление расписания уходит от недели до двух;
* ошибки приводят к необходимости переделки всего расписания.

## Решение

Автоматический алгоритм «ШколаПлан»:
* загружает список учеников с выбранными предметами;
* учитывает кабинеты и учителей для каждой группы;
* поддерживает ИУП и разделение параллели по общим предметам;
* генерирует расписание за 10 минут;
* позволяет корректировать составленный план.

## Цели текущей версии

* подтвердить сокращение времени составления расписания до 1–2 дней (с учётом корректировки, целевое сокращение — 85 %);
* привлечь 5 пилотных школ;
* собрать данные для улучшения алгоритма.

## Метрики успеха

| Параметр | Способ измерения | Целевое значение |
|--------|---------------|-----------------|
| Время составления расписания | Сравнение времени до и после внедрения | Сокращение на 85 % (с 14 до 1–2 дней) |
| Количество итераций | Подсчёт числа корректировок после первого создания расписания | Минимизация количества итераций |
| NPS завучей | Опрос по шкале от 0 до 10 | > 8 из 10 |
| Готовность рекомендовать | Вопрос «Будете ли вы рекомендовать продукт коллегам?» | Ответ «да» от большинства респондентов |
| Готовность платить | Указание суммы в рублях, которую готовы заплатить за продукт | Сбор данных о приемлемом ценовом диапазоне |

## Минимальный функционал (MVP)

Обязательные функции:
* импорт учеников из Excel (ФИО + выбранные предметы);
* визуальный конструктор групп (drag‑and‑drop);
* проверка конфликтов в реальном времени;
* генерация расписания расписания без ИУП
* экспорт готового расписания в Excel.

Не включается в MVP:
* мобильные приложения;
* дополнительные интеграции;
* учет индивидуальных учебных планов.

## Подход к созданию дешёвого MVP (имитационное решение)

Этапы реализации:
1. Сбор требований от учителей через Google Формы.
2. Внесение данных в Excel и составление расписания с использованием макросов VBA.
3. Ручная доработка результатов (учёт особых случаев, корректировка конфликтов).
4. Визуализация результатов в удобном для заказчика виде (таблицы, графики, пояснения).
5. Предоставление результатов пилотным школам и сбор обратной связи.

## План действий

1. Разработать Google‑форму для сбора требований от учителей.
2. Создать шаблон Excel с макросами VBA для генерации расписания.
3. Провести пилотное тестирование с 1–2 школами.
4. Собрать обратную связь и доработать шаблон.
5. Привлечь ещё 3–4 школы для расширенного тестирования.
6. Проанализировать данные и сформировать план доработки полноценного алгоритма.

## Ожидаемые результаты пилотного запуска

* подтверждение гипотезы о сокращении времени составления расписания;
* получение отзывов от завучей о удобстве и полезности решения;
* сбор данных о наиболее частых конфликтах и сложностях при составлении расписания;
* формирование базы для разработки полноценного алгоритма.

## ОСНОВНЫЕ ФУНКЦИИ
**Функция 1. Импорт и «оживление» расписания**
* Методист загружает текущее расписание из Excel, CSV или копирует таблицу из браузера. Система парсит данные, распознаёт структуру (классы, предметы, учителя, кабинеты) и строит интерактивную модель школы. Умный парсер с нечётким сопоставлением: «Иванова М.П.» и «Иванова Мария Петровна» — один человек. Шаблоны импорта для популярных форматов (1С:АСР, Ректор, типовой Excel).
**Функция 2. СанПиН-аудит в один клик** КИЛЛЕР-ФИЧА
* После импорта методист нажимает «Проверить» и за 15 секунд получает полный аудит по 47 правилам СанПиН 1.2.3685-21. Результат — визуальный дашборд: каждый класс, каждый день, каждое правило. Зелёное — соответствует, жёлтое — рекомендация нарушена, красное — жёсткое нарушение. Отчёт со ссылками на пункты нормативов можно скачать в PDF и показать проверяющим.
**_Методист получает ценность за 30 секунд, ещё до того как что-либо оптимизировал. Это бесплатная функция и точка входа в продукт (PLG-воронка)._**
**Функция 3. Экстренные замены с AI-подбором**
* Завуч отмечает отсутствие учителя. Система за 5 секунд предлагает план замен — ранжированный список кандидатов для каждого урока. Для каждого варианта: ФИО, квалификация, текущая нагрузка, свободен ли в нужный слот, доступен ли кабинет, побочные эффекты («появится окно»). После подтверждения — готовая карточка замен (PNG/PDF) для стенда и чата в Сферуме.
Техническая основа: CP-SAT solver (Google OR-Tools) с warm start — фиксация неизменённых уроков, пересчёт только затронутых. Время от фиксации отсутствия до уведомления всех: 3 минуты вместо 45.
**Функция 4. Конструктор ИУП и автосборка групп** (не входит в mvp)
Три шага.
* (1) Сбор выборов: система генерирует веб-форму, ученики выбирают предметы и уровни, система проверяет ФГОС-ограничения.
* (2) Кластеризация: из 120 индивидуальных наборов формируются 8–12 оптимальных учебных групп; методист видит тепловую карту и может корректировать drag-and-drop.
* (3) Генерация расписания для сформированных групп без конфликтов и с соблюдением СанПиН.
Результат: 1 день вместо 2–3 недель. Доля учеников с полностью бесконфликтным расписанием: >95%.
**Функция 5. Поиск слотов для проектных групп** (не входит в mvp)
Руководитель проекта задаёт состав межклассной группы и требования (кабинет с проектором, 2 спаренных урока). Система находит общие свободные слоты, визуализирует доступность в виде тепловой карты и предлагает минимальные корректировки основного расписания при необходимости. Покрывает требование ФГОС по обязательной проектной деятельности.


## КЛИЕНТСКИЙ ПУТЬ
**Фаза 0. Обнаружение**
* Август, конец лета. Методист Елена Сергеевна ищет «составить расписание школа/проверка расписания СанПиН онлайн». Находит лендинг SmartSchedule: «Составьте расписание за 30мин/Загрузите расписание — получите аудит за 30 секунд. Бесплатно.»
**Фаза 1. Первая ценность за 2 минуты**
* Загружает Excel. Парсер распознаёт 38 классов, задаёт 2 уточняющих вопроса. Через 15 секунд — дашборд: 12 классов с нарушениями, конкретные проблемы, ссылки на пункты нормативов. Скачивает PDF, показывает директору.
**Фаза 2. «Оживление» расписания (Trial, день 1)**
* Регистрируется на 14-дневный trial. Проходит по 12 нарушениям: система предлагает исправления, Елена принимает или корректирует. За 30 минут — все красные маркеры стали зелёными. «Раньше на это уходил целый день.»
**Фаза 3. Экстренная замена (Trial, неделя 2)**
* Вторник, 7:20. Учитель физики на больничном, 4 урока. Елена отмечает отсутствие → за 5 секунд план замен с объяснениями → подтверждение в один клик → карточка замен в чат Сферума и на стенд. 3 минуты вместо 45.
**Фаза 4. ИУП для 10-х классов (Октябрь)**
* Директор запускает профильное обучение: 4 класса, 112 учеников. Елена создаёт форму выбора, ученики заполняют за неделю. Нажимает «Сформировать группы» — за 40 секунд из 30+ уникальных наборов формируются 9 групп. 104 ученика без конфликтов, для 4-х — варианты решения. Запускает генерацию расписания: 3 минуты. Раньше: 2–3 недели.
**Фаза 5. Конверсия**
* На 12-й день trial’а Елена получает письмо с персональной статистикой: 12 исправленных нарушений, 7 замен за среднее время 4 минуты, ИУП для 112 учеников. Подписка — 14 400 ₽/год. Директор сравнивает с лицензией Ректора (17 000 ₽/год) без замен, без ИУП, без веба. Решение очевидно.  

# Структура проекта ШколаПлан в Git

Репозиторий: **`github.com/DianaKapri/IU-Startup`**, основная ветка `main`.

---

## Корень репозитория

```
IU-Startup/
├── server.js              ← главный сервер (port 5000)
├── package.json           ← зависимости главного сервера
├── docker-compose.yml     ← оркестрация (frontend + backend + CP-SAT)
├── Dockerfile             ← Docker-образ для деплоя
├── Caddyfile              ← reverse-proxy + HTTPS (на проде)
├── replit.md / CLAUDE.md  ← инструкции для AI-инструментов разработки
├── README.md / CHANGELOG  ← общая документация
│
├── projects/              ← основной код приложения
├── cp-sat-solver/         ← Python-сервис генерации расписаний (OR-Tools)
├── assets/                ← статические ассеты репо (логотипы, иконки)
├── docs/                  ← техническая документация
├── notes/                 ← внутренние заметки команды
├── prompts/               ← наработки промптов для AI-задач
└── attached_assets/       ← вложения, скриншоты для документации
```

`server.js` в корне — точка входа. Он слушает порт 5000, отдаёт фронтенд
статикой через `express.static('projects/frontend')` и проксирует все
запросы `/api/*` на бэкенд (порт 4000). То есть когда браузер делает
запрос `/api/captcha`, реально его обрабатывает бэкенд, но клиент об
этом не знает — для него всё с одного домена.

---

## projects/ — основной код

Три подсистемы:

```
projects/
├── frontend/              ← клиентская часть (HTML/CSS/JS, без билда)
├── sanpin-audit-ui/       ← бэкенд на Node.js/Express (port 4000)
└── services/              ← общие вспомогательные сервисы
```

Принципиальное разделение фронта и бэка — фронт работает в браузере
пользователя, бэк работает на сервере. Они общаются только через HTTP
по `/api/*`. Никакого SSR/Next.js — простой статический сайт + REST API.

---

## projects/frontend/ — клиент

```
frontend/
├── index.html             ← главная страница (лендинг)
├── login.html             ← вход и регистрация
├── account.html           ← личный кабинет (главная страница после входа)
├── subscription.html      ← страница оплаты подписки
├── audit-view.html        ← страница просмотра одного аудита из истории
├── demo.html              ← демонстрационный аудит (без регистрации)
├── compare.html           ← сравнение двух расписаний
├── wizard.html            ← старый wizard составления расписания
├── generator.html         ← старый генератор расписаний
├── reset-password.html    ← восстановление пароля
├── onboarding.html        ← первый вход после регистрации
├── contacts.html          ← контактная страница
├── admin.html / admin-login.html ← админ-панель управления заявками
├── schedule.html          ← рендер расписания
│
├── js/                    ← JavaScript-модули
│   ├── auth.js            ← вход/регистрация через Supabase, профиль
│   ├── account.js         ← логика личного кабинета
│   ├── engine.js          ← движок аудита СанПиН (23 проверки, scoring)
│   ├── scripts.js         ← общие утилиты + старый wizard
│   ├── pricing.js         ← загрузка цен с /api/pricing
│   ├── paywall.js         ← модалка «купите подписку»
│   └── captcha.js         ← виджет встроенной капчи
│
├── css/                   ← стили (без препроцессора)
│   ├── styles.css         ← глобальные стили + компоненты лендинга
│   ├── account.css        ← стили ЛК
│   ├── account-cards.css  ← карточки на старт-экране ЛК
│   ├── demo.css           ← стили демо-страницы
│   ├── paywall.css        ← модалка paywall
│   └── subscription.css   ← страница оплаты
│
└── assets/                ← картинки, шрифты, шаблоны xlsx
    ├── shablon-raspisaniya.xlsx   ← шаблон для аудита (3 листа)
    └── generator-template.xlsx     ← шаблон для составления расписания
```

**Принципы организации фронта:**

- HTML-страниц много, потому что архитектура многостраничная (MPA),
  а не SPA. Каждая страница — отдельный entry-point.
- JS подключается тегами `<script src=...>` напрямую, без сборки.
- `engine.js` — самый большой файл во фронте, в нём вся логика аудита
  СанПиН: правила C-01..C-03, E-01..E-03, X-01, scoring 0–100,
  рендеринг тепловой карты.
- `auth.js` — обёртка над Supabase JS SDK с локализацией ошибок.

---

## projects/sanpin-audit-ui/ — бэкенд

```
sanpin-audit-ui/
├── app.js                 ← точка входа Express (port 4000)
├── package.json           ← зависимости бэкенда
│
├── routes/                ← обработчики HTTP-эндпоинтов
│   ├── auth.js            ← POST /api/auth/register (+ admin login)
│   ├── users.js           ← GET /api/users/me (профиль с планом)
│   ├── schedules.js       ← CRUD расписаний в БД
│   ├── generator.js       ← POST /api/generate (старый CP-SAT генератор)
│   ├── subscriptions.js   ← POST /api/subscription-request (оплата)
│   ├── payments.js        ← webhook YooKassa
│   ├── adminSubscriptions.js ← админка заявок
│   ├── captcha.js         ← GET /api/captcha (выдача challenge)
│   ├── captchaVerify.js   ← POST /api/captcha/verify (предпроверка)
│   └── protectedScripts.js   ← signed-URL для generator-v2.js
│
├── services/              ← бизнес-логика (вне HTTP)
│   ├── audit/             ← аудит СанПиН (зеркало engine.js, для генератора)
│   │   ├── checks.js      ← правила C-01..X-01
│   │   └── scoring.js     ← формула 0–100
│   ├── parser/            ← парсеры Excel
│   │   ├── index.js       ← роутер форматов (5 стратегий)
│   │   ├── try-parse-audit-template.js  ← основной формат
│   │   ├── try-parse-rows.js / try-parse-transposed.js
│   │   └── norm-subj.js   ← нормализация названий предметов
│   ├── template/          ← генерация xlsx-шаблонов
│   ├── payment/yokassa.js ← интеграция YooKassa
│   ├── sanpin-norms.js    ← НСИ из СанПиН 1.2.3685-21
│   └── sanpin-rules.json  ← данные шкал трудности предметов
│
├── middleware/            ← Express middleware
│   ├── requirePlan.js     ← проверка plan='paid' (Supabase JWT + БД)
│   ├── requireAdmin.js    ← проверка X-Admin-Token
│   ├── captcha.js         ← verifyCaptcha (honeypot + minTime + HMAC)
│   └── upload.js          ← multer для загрузки файлов
│
├── protected/             ← файлы, отдаваемые только по signed URL
│   └── generator-v2.js    ← фронтовый генератор (1218 строк)
│                            доступен через /api/scripts/generator-v2.js?token=...
│
├── config/                ← конфигурация
│   ├── database.js        ← подключение к PostgreSQL (Supabase)
│   └── pricing.js         ← цены тарифа Школа
│
├── migrations/            ← SQL-миграции БД
│   ├── 001-init.sql       ← schools, users, schedules
│   ├── 002-schedules-audit.sql
│   ├── 003-payment-flow.sql
│   └── 004-schedules-public-share.sql
│
└── tests/                 ← unit-тесты (node --test)
    ├── checks.test.js     ← правила СанПиН
    ├── scoring.test.js    ← формула баллов
    ├── adminSubscriptions.test.js
    ├── requirePlan.test.js
    └── verifySupabaseToken.test.js
```

**Принципы организации бэка:**

- **`routes/` хранит только HTTP-обвязку** (парсинг параметров, ответы).
  Логика — в `services/`. Это упрощает тестирование: тесты дёргают
  функции из `services/` напрямую, минуя Express.
- **Нет папки `controllers/`** — это сознательное решение, чтобы не
  размазывать логику по трём слоям. `routes/` — это и есть тонкая
  «контроллер»-обвязка.
- **Миграции — числовые, по порядку применения**. Не используем
  миграционные библиотеки (Knex/Sequelize) — простые SQL-файлы
  применяются вручную через Supabase Dashboard.

---

## cp-sat-solver/ — Python-сервис генерации

```
cp-sat-solver/
├── solver.py           ← основная функция, обёртка CP-SAT
├── builder.py          ← сборка модели OR-Tools из входных данных
├── sanpin.py           ← наложение ограничений СанПиН на модель
├── models.py           ← Pydantic-схемы входа/выхода
├── fixtures/           ← тестовые JSON-входы для отладки
└── requirements.txt    ← Python-зависимости (ortools, fastapi)
```

Отдельный процесс на Python, потому что CP-SAT solver (Google OR-Tools)
— это C++/Python библиотека, в Node.js её нет. Запускается на порту 8000,
бэкенд `/api/generate` дёргает его через HTTP.

Сейчас этот старый flow устарел: новая генерация (`generator-v2.js`)
работает прямо в браузере, без CP-SAT. Но Python-сервис оставлен в
репо как fallback и для возможного возврата к серверной генерации.

---

## Где какой README

- **`README.md`** — общее описание для GitHub
- **`replit.md`** — инструкция для AI-инструментов (Cursor/Claude)
  по архитектуре. Самый актуальный документ для понимания дизайн-решений.
- **`CLAUDE.md`** — то же, специфично для Claude Code
- **`CHANGELOG`** — лог релизов
- **`docs/deployment.md`** — как разворачивать на проде
- **`docs/plans/`** — планы продукта, roadmap
- **`notes/`** — внутренние заметки команды (CustDev, UX-итерации)

---

## Что куда добавлять

| Задача | Куда |
|---|---|
| Новый эндпоинт API | `projects/sanpin-audit-ui/routes/` + регистрация в `app.js` |
| Новое правило аудита | `projects/sanpin-audit-ui/services/audit/checks.js` **И** `projects/frontend/js/engine.js` (два места — frontend дублирует логику) |
| Новая страница лендинга | `projects/frontend/*.html` |
| Изменение модели БД | новый файл в `projects/sanpin-audit-ui/migrations/00X-...sql` |
| Защита нового роута подпиской | `middleware/requirePlan(['paid'])` |
| Новая ENV-переменная | задокументировать в `replit.md` и `docs/deployment.md` |

---

## Workflow Git

- Все коммиты идут в `main` через feature-ветки.
- Названия веток с префиксами: `feat/`, `fix/`, `chore/`, `docs/`, `refactor/`.
- Каждый коммит — production-ready: всегда есть рабочее состояние.
- Сторонние правки коллегой (Дианой) часто параллельные, делаем
  `git pull --rebase` перед слиянием.

Стек на проде:

- **Node.js + Express** (бэк), статика отдаётся express.static
- **PostgreSQL** через Supabase (хранилище + auth)
- **Python + OR-Tools** для CP-SAT генерации (опционально)
- **YooKassa** для приёма платежей
- **Resend** для отправки email
- **Replit** для хостинга (dev/staging), Docker-стек для прода

